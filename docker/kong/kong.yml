# Generated by scripts/render-kong-compose.py; edit Helm values/templates.
_format_version: "3.0"
_transform: true
services:
  - name: auth-service
    host: auth-service
    port: 8001
    protocol: http
    connect_timeout: 5000
    read_timeout: 15000
    write_timeout: 15000
    retries: 0
    routes:
      - name: auth-api
        paths:
          - "~/api/v1/auth(?:/|$)"
        strip_path: false
        preserve_host: false
      - name: auth-sensitive
        paths:
          - '~/api/v1/auth/(?:login|register|refresh)/?$'
        methods: [POST]
        regex_priority: 20
        strip_path: false
        plugins:
          - name: rate-limiting
            config:
              minute: 10
              policy: local
              limit_by: ip
              hide_client_headers: true
  - name: auth-health
    host: auth-service
    port: 8001
    protocol: http
    path: /health
    connect_timeout: 5000
    read_timeout: 15000
    write_timeout: 15000
    retries: 0
    routes:
      - name: auth-health
        paths:
          - "~/api/v1/auth/health$"
          - '~/health$'
        methods: [GET, OPTIONS]
        regex_priority: 100
        strip_path: true
  - name: chat-service
    host: chat-service
    port: 8004
    protocol: http
    connect_timeout: 5000
    read_timeout: 15000
    write_timeout: 15000
    retries: 0
    routes:
      - name: chat-api
        paths:
          - "~/api/v1/chats(?:/|$)"
        strip_path: false
        preserve_host: false
  - name: chat-health
    host: chat-service
    port: 8004
    protocol: http
    path: /health
    connect_timeout: 5000
    read_timeout: 15000
    write_timeout: 15000
    retries: 0
    routes:
      - name: chat-health
        paths:
          - "~/api/v1/chats/health$"
        methods: [GET, OPTIONS]
        regex_priority: 100
        strip_path: true
  - name: notification-service
    host: notification-service
    port: 8005
    protocol: http
    connect_timeout: 5000
    read_timeout: 15000
    write_timeout: 15000
    retries: 0
    routes:
      - name: notification-api
        paths:
          - "~/api/v1/notifications(?:/|$)"
        strip_path: false
        preserve_host: false
  - name: notification-health
    host: notification-service
    port: 8005
    protocol: http
    path: /health
    connect_timeout: 5000
    read_timeout: 15000
    write_timeout: 15000
    retries: 0
    routes:
      - name: notification-health
        paths:
          - "~/api/v1/notifications/health$"
        methods: [GET, OPTIONS]
        regex_priority: 100
        strip_path: true
  - name: post-service
    host: post-service
    port: 8003
    protocol: http
    connect_timeout: 5000
    read_timeout: 15000
    write_timeout: 15000
    retries: 0
    routes:
      - name: post-api
        paths:
          - "~/api/v1/posts(?:/|$)"
          - "~/api/v1/users(?:/|$)"
        strip_path: false
        preserve_host: false
  - name: post-health
    host: post-service
    port: 8003
    protocol: http
    path: /health
    connect_timeout: 5000
    read_timeout: 15000
    write_timeout: 15000
    retries: 0
    routes:
      - name: post-health
        paths:
          - "~/api/v1/posts/health$"
        methods: [GET, OPTIONS]
        regex_priority: 100
        strip_path: true
plugins:
  - name: cors
    config:
      origins:
        - "http://localhost:3000"
        - "http://localhost:3001"
      methods: [GET, POST, PATCH, PUT, DELETE, OPTIONS, HEAD]
      headers: [Authorization, Content-Type, X-CSRF-Token, X-CSRF-Protection, X-Request-ID]
      exposed_headers: [X-Request-ID, Retry-After, RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset]
      credentials: true
      max_age: 600
      preflight_continue: false
  - name: rate-limiting
    config:
      minute: 100
      policy: local
      limit_by: ip
      hide_client_headers: true
  - name: request-size-limiting
    config:
      allowed_payload_size: 16
      size_unit: kilobytes
      require_content_length: false
  - name: correlation-id
    config:
      header_name: X-Request-ID
      generator: uuid
      echo_downstream: true
